// Marker classes for the Logistics services, resolved on the engine (engine.resolve(BeltService)).

/**
 * Key for the belt transport (the Belts module instance): paths, placement, tile lookups.
 */
export class BeltService {

}

/**
 * Key for the splitter behavior instance: sim-only splitter construction for specs/debugging.
 */
export class SplitterService {

}

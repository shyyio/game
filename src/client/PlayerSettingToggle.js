import {AbstractPlayerSettingControl} from "@/client/AbstractPlayerSettingControl.js";

/**
 * An on/off switch; the setting holds SETTING_ON/SETTING_OFF (absent = on).
 */
export class PlayerSettingToggle extends AbstractPlayerSettingControl {
}

export class TextureDefinition {

    /**
     * @param {string} imageUrl
     * @param {string} jsonUrl
     */
    constructor(imageUrl, jsonUrl) {
        this.imageUrl = imageUrl;
        this.jsonUrl = jsonUrl;
    }
}
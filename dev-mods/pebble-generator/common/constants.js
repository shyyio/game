// The numbers this mod invents. Item types and tool ids are shared with every other mod in a
// loadout, so pick values nobody else uses.
export const ITEM_TYPE_PEBBLE = 9000;
export const TOOL_ID_PEBBLE_GENERATOR = 900;

// Ticks between pebbles.
export const GENERATOR_TICKS = 8;
